<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  android:versionCode="1"
		  android:versionName="0.2" 
		  package="com.andybotting.tramhunter">
		  <!-- Also set version in strings.xml -->
	  
	<application android:icon="@drawable/icon" 
				 android:label="@string/app_name" 
				 android:theme="@android:style/Theme.Light">
		
		<activity android:name=".TramHunter"
				  android:label="@string/app_name" > 
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity android:name=".EnterTTIDActivity" android:label="@string/app_name" />
		<activity android:name=".RoutesListActivity" android:label="@string/app_name" />
		<activity android:name=".NearStopsActivity" android:label="@string/app_name" />
		<activity android:name=".StopsListActivity" android:label="@string/app_name" />
		<activity android:name=".StopDetailsActivity" android:label="@string/app_name" />
		<activity android:name=".StopMapActivity" android:label="@string/app_name" />
		
		<uses-library android:name="com.google.android.maps" />
		   
	</application>
	
	
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	
	<uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS"></uses-permission>
	<uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION"></uses-permission>
	
	<uses-sdk android:minSdkVersion="3" />
	
</manifest> 
